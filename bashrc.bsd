unalias rm
unalias diff
unalias lsblk

alias pacu='pkg upgrade'
if which gnuls > /dev/null; then
  alias ls='gnuls --color=auto'
elif which exa > /dev/null; then
  alias ls=exa
else
  alias ls='ls -G'
fi
